# Development Plan Template (Dt)

## Before Planning

Read the prior evidence packet at `evidence/E_t.json` (or the latest
`evidence/E_<n>.json` if resuming mid-iteration) plus `evidence/INDEX.md` before writing
this iteration's plan. A summarized note in `activeContext.md` is not a substitute for
reading the packet itself (H-1).

## Preservation Gate

- Player input changes avatar motion (`player_control`, verified iteration 1).

## Update Targets

- Implement a visible completion state.
